# Qlink Mediator Backend (FastAPI)
# ------------------------------
# Copy this file to `.env` for local development.
# Do NOT commit `.env` (it contains machine-specific values).

# Local server settings (used only when running `python api_server.py`)
QLINK_HOST=127.0.0.1
QLINK_PORT=8000
QLINK_RELOAD=1

# Where uploaded files + run logs are stored.
# Local: keep as `uploads`
# Vercel: use `/tmp/uploads` (ephemeral, not persistent)
QLINK_UPLOADS_DIR=uploads

# Reconciliation script entrypoint
QLINK_RECONCILE_SCRIPT=main.py

# CORS (frontend origins allowed to call the API)
QLINK_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://clara-frontend-sigma.vercel.app

# Timeout for reconciliation subprocess (seconds)
QLINK_RECONCILE_TIMEOUT_SECONDS=900

# Optional: Serve built frontend (only if you build it into Qlink/dist)
QLINK_SERVE_FRONTEND=0
